dist: bionic

# branches:
#    only:
#       - master

matrix:
   include:
      -  os: linux
         compiler: gcc
         language: cpp

         install:
            - python --version
            - pip --version
            - python -m pip install boto3
            - python -m pip install --upgrade requests
            - pip show requests
            - pip show urllib3
            - pip show chardet


script:
   - python check.py
